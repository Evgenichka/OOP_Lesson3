import java.util.List;

public class Library {
      static class library<string>
        {
            public List<Book> books = new List<Book> { 
            private string filterTitle;
            public List<Book> fnd(string string title;
            string title1 = title;);
            {
                filterTitle = title1;
                Object PredicateTitle = null;
                boolean res = books.addAll(null);
                return res;
            }
            private bool PredicateAuthor(Book bk)
            {
                if (bk.title == filterTitle )
                    return true;

                else
                    return false;
            }

        }

            public List<Book> fnd(String s) {
            }
